# CLAUDE.md

정영준의 웹 이력서 (Next.js). 이력서 내용은 `src/data/*.ts`, 소개글은 `src/app/components/ResumeHeader.tsx`에 있다.

## 작업 규칙

- **브랜치**: `master`만 관리한다. `master`에 푸시하면 바로 배포되므로 푸시 전에 확인받는다.
- **커밋**: 작업 단위로 나눠서 한국어로 쓰고, 접두사를 붙인다 (`feat:` / `fix:` / `refactor:` / `chore:`).
- 사실관계(수치, 날짜, 직함, 진행 상태)는 추측해서 쓰지 말고 물어본다.

## 공통 작성 규칙

- **강조**는 `StrongComponent`(노란 배경) 하나로 통일한다. 문장 전체가 아니라 **핵심 구절만** 강조하고, 데이터에서는 `**구절**`로 감싼다 (`strong: true`는 쓰지 않는다).
- 강조는 적을수록 좋다. 소개글은 문단당 1개, 프로젝트는 대개 1개만 강조한다.
- 자격증처럼 날짜가 있는 목록은 최신순으로 둔다.
- 목록을 나열할 때 구분 기호는 `·`를 쓴다 (`/`는 쓰지 않는다).
- 한 문단 안에서 같은 동사를 반복하지 않는다.

### 용어 통일

| 대상 | 쓰는 표현 |
|---|---|
| 웹-앱 연동 | `WebView ↔ 네이티브 브릿지` (KGM Music만 `네이티브(AAOS)`) |
| 차량 서비스 | `인포테인먼트` (영문 `Infotainment` 쓰지 않음) |
| 웹 애플리케이션 | `웹앱` (`WebApp` 쓰지 않음) |
| 근무일지 입력 데이터 | `JIRA 워크로그` (`티켓` 아님) |
| Claude Code 스킬화 규격 | `백엔드 API·네이티브 브릿지·음성발화·차량 제어` |
| 진행 상태 | `진행 중` (띄어 씀) |
| NAN 해커톤 | `NAN 2026 Game X AI 해커톤` (공식 명칭) |
| 역할명 | 영문 직함 그대로 (`Frontend Developer`, `Full-stack Developer`) |

## 소개글 (ResumeHeader)

문단마다 역할이 하나씩 있다.

1. **정체성과 태도**: 5년차 프론트엔드 개발자라는 소개에 이어, 누군가는 해야 하는 일에 먼저 나서고 끝까지 책임지는 태도를 쓰고 인사평가 결과로 뒷받침한다.
2. **AI 활용**: 주제 문장(`일하는 모든 과정에 AI를 깊이 녹여 냅니다`)을 먼저 쓰고, 구체적인 사례를 뒤에 붙인다.
3. **그 밖의 성과**: `이 밖에도`로 시작해 해커톤과 특허를 쓴다.

## 경력 (workExperiences.ts)

- **회사 설명**: 1~2문장으로 쓴다.
- **프로젝트 설명**: 1~2문장으로 쓰고 마침표로 끝낸다. 설계 배경을 쓸 때는 기능보다 "왜 이렇게 만들었는지"를 사용자 상황 중심으로 쓴다.
- **성과 항목**: 마침표 없이 명사형으로 끝낸다 (`~설계`, `~구축`, `~단축`).
  - 프로젝트당 가장 차별화되는 성과의 핵심 구절만 강조한다.
  - "API 연동"처럼 어느 프로젝트에나 들어갈 수 있는 문구는 쓰지 않는다.
  - 여러 프로젝트에 같은 성과가 있으면 한 곳에만 남긴다.
  - "효율 향상"처럼 막연하게 쓰지 말고, 무엇이 달라졌는지를 쓴다.
  - 수치는 실제로 측정된 것만 쓴다. 테스트 개수처럼 의미가 약한 수치는 넣지 않는다.
- **기간 표기**: `YYYY.MM - YYYY.MM` 또는 `YYYY.MM - 진행 중`으로 쓴다. 짧은 프로젝트는 `YYYY.MM (1주)`로 쓴다. 유지보수 단계에 들어갔어도 개발 중인 프로젝트는 `진행 중`으로 둔다.
- **순서**: 프로젝트는 최신순으로 둔다.

## Activities (activities.ts)

- 경력과 같은 작성 규칙을 따른다.
- 이번 지원(게임 기획자)에 맞춰 **기획 기여를 먼저** 쓰고, 구현 이야기는 뒤에 짧게 둔다.
- 역할에 기획 참여 여부를 드러낸다 (`공동 기획`, `기획`).
- 기획 성과가 여러 개인 항목(ObiVision)은 성과마다 핵심 구절을 강조해도 된다.

## 확인된 사실 (다시 묻지 않아도 되는 것)

- 근무일지 솔루션은 **전사 도입**이 완료됐다. `작성 시간 90% 단축` 수치도 지금 방식에서 유효하다. 기간은 최초 개발 기준인 `2025.10 (1주)`로 둔다.
- `현대제네시스 전무 대상 데모`는 표기가 맞다.
- 인사평가는 **4년 연속** 전사 상위 2% 최고 등급이다.
- 특허는 **사내 직무발명**이다. 변리사가 작성한 출원명세서를 전달받은 단계까지 확인됐다. 출원 시점은 정해지지 않았으니 날짜를 쓰지 않는다.
- NAN 2026: 주제 소재 '2026년'을 자산과 연결한 해석은 본인 아이디어다. AI 개발 방식은 초기 코드를 멀티 에이전트 워크플로우로 만들고, 이후 이슈는 Fable이 계획하고 Opus·Sonnet이 수정했다. 팀 커밋 수는 넣지 않는다.
- ObiVision: 본인이 팀장이다. 핵심 기여는 3D 파손 시각화에서 수리비 견적서 표준으로의 방향 전환이다.
- 다음 항목은 의도적으로 둔 것이다.
  - 연락처는 넣지 않는다.
  - 롯데마트 경력은 설명 없이 둔다.
  - TOEIC(만료)과 GTQ는 가진 자격을 보여주기 위해 그대로 둔다.
- 이번 이력서는 NHN 게임 기획자 패스트트랙(NAN 2026 수상) 지원용이다. 다만 기획자용으로 새로 쓰지 않고, 지금까지 해 온 일을 그대로 보여주는 방향을 유지한다.
